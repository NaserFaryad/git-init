git remote add origin https://github.com/NaserFaryad/git-init.git

git remote -v

git push

git pull

git status

git pull --allow-unrelated-histories

# to switch to a branch
git checkout <branch name>

# to create a branch and switch to i it
git checkout -b <new branch name>

# to create a new branch
git branch <new branch name>

# to show all branches
git branch

// Merge branches \\
#Merge Alpha branch to Master branch
git merge alpha

#delete alpha branch
git branch -d alpha

# when another person created a new branch we should FETCH it to use it in our PC
git fetch origin

#ignoring some files with .gitignore file



